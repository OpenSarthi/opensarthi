from typing import Optional

async def describe_screen(image_bytes: bytes) -> Optional[str]:
    """
    Optional: Describe screen using multimodal LLM.
    Returns None for now, to be extended with Gemini Vision.
    """
    return None
